# 🔍 诊断指南：下载失败问题排查

## 问题现象
插件显示 **"成功 0 / 失败 30"**，所有歌曲都下载失败。

---

## 排查步骤

### 第一步：查看详细错误日志

1. 打开 Chrome，点击右上角 **三个点（⋮）** → **更多工具** → **开发者工具**
2. 切换到 **Console**（控制台）标签
3. 点击插件图标，重新运行一次下载
4. 观察控制台输出的红色错误信息

### 重点关注的日志

#### 日志 A：`[BGM] music detail API 响应`
```
[BGM] music detail API 响应: {itemId: "...", response: {...}}
```
- 展开 `response` 对象，查看里面的结构
- 截图发给技术人员

#### 日志 B：`[BGM] 无法提取 MP3 URL`
```
[BGM] 无法提取 MP3 URL，API 响应结构: {...}
```
- 说明抖音 API 返回的数据结构变了
- 截图完整的 JSON 内容

#### 日志 C：`[BGM] 失败详情`
```
[BGM] 失败详情: [{rank: 1, title: "歌名", reason: "..."}]
```
- 记录 `reason` 字段的内容

---

### 第二步：检查网络请求

1. 开发者工具切换到 **Network**（网络）标签
2. 勾选 **Preserve log**（保留日志）
3. 重新运行插件
4. 在过滤框输入 `music/detail`
5. 找到请求，点击查看：
   - **Headers**：检查 Status Code（应该是 200）
   - **Response**：查看返回的 JSON 内容
6. 截图发给技术人员

---

### 第三步：验证登录状态

1. 新标签页打开 `https://creator.douyin.com`
2. 确认已登录（能看到个人头像）
3. 点击左侧菜单 **创作灵感** → **热门音乐**
4. 确认能看到榜单列表
5. 如果看不到或提示登录，重新扫码登录

---

## 常见原因和解决方案

### 原因 1：抖音 API 数据结构变更 ⚠️ 最可能
**症状：** 控制台显示 `MP3 URL 为空`

**说明：** 抖音修改了 `music/detail` 接口的返回格式，原代码中的路径 `music_info?.play_url?.url_list?.[0]` 失效。

**解决：** 需要技术人员根据新的 API 响应结构更新代码。

---

### 原因 2：需要新的请求头或 Cookie
**症状：** API 返回 403 或 401

**解决：** 
- 确保在 `creator.douyin.com` 已登录
- 清除浏览器缓存后重新登录

---

### 原因 3：MP3 URL 需要额外认证
**症状：** 拿到了 MP3 URL，但下载时失败

**解决：** 需要在下载时添加特殊的 referer 或 header。

---

### 原因 4：网络代理或防火墙
**症状：** 请求超时或连接失败

**解决：**
- 关闭 VPN/代理
- 检查公司网络是否拦截了抖音域名

---

## 临时诊断版本说明

当前版本已添加详细日志，会在控制台输出：
- 每次 API 调用的完整响应
- 每首失败歌曲的具体原因
- 数据解析失败时的原始 JSON

**请将控制台截图发给技术人员，包含：**
1. 红色的错误信息
2. `[BGM] music detail API 响应` 的展开内容
3. Network 面板中 `music/detail` 请求的 Response

---

## 联系技术支持

整理以下信息发给技术人员：
- [ ] 控制台截图（包含所有红色错误）
- [ ] Network 面板截图（music/detail 请求的 Response）
- [ ] 能否正常访问创作者平台热门音乐页面
- [ ] Chrome 版本号（地址栏输入 `chrome://version/`）
- [ ] 插件版本号（扩展管理页面查看）
