# 🎵 抖音 BGM 采集器 · 新手使用说明

**读完这份，从零开始 5 分钟能用上。**

装一次即可，之后每次下 BGM 就 3 步：**点图标 → 选参数 → 点开始**。

---

## ✅ 你需要什么

- 一台电脑（Windows 或 Mac 都行）
- 装了 **Chrome 浏览器**（没有的话去 <https://www.google.com/chrome/> 下载安装，免费）
- 一个 **抖音账号**（个人号即可，用手机扫码登录）

**不需要**：任何编程知识、命令行、Python、开发者账号。

---

## 📦 第一步：解压插件

你收到的是一个 zip 压缩包，比如 `douyin-bgm-extension-v1.0.zip`。

1. 在 zip 文件上**点右键**
2. 选「**解压到当前文件夹**」（或「Extract Here」）
3. 会得到一个叫 `douyin-bgm-extension` 的**文件夹** ← **记住它在哪，等下要用**

> ⚠️ **一定要先解压再装**！直接双击 zip 里的文件没用。

---

## 🔌 第二步：装到 Chrome（4 小步）

### 2.1 打开 Chrome 扩展管理页
- 打开 Chrome
- 在最顶上的地址栏里（就是平时输网址的那一行），**输入这个然后回车**：
  ```
  chrome://extensions/
  ```

### 2.2 开启「开发者模式」
- 页面**右上角**有一个「开发者模式」的开关
- 点一下把它打开（变蓝色 = 已开）
- 页面左上会多出来 3 个按钮

### 2.3 加载插件
- 点最左边那个 **「加载已解压的扩展程序」**
- 弹出的窗口里，找到你刚才**解压出来的 `douyin-bgm-extension` 文件夹**
- **⚠️ 关键：选中这个文件夹本身**（不要双击进去），然后点「选择文件夹」/「Select Folder」
- 页面上会出现一张卡片：「**抖音BGM采集器 v1.0.0**」 = 装好了 🎉

### 2.4 把插件图钉固定到右上角（可选，但强推）
- Chrome 最右上角有个**拼图形状图标**（🧩）
- 点它 → 找到「抖音BGM采集器」→ 点右边**图钉图标（📌）**
- 之后 Chrome 右上角就能一直看到它

---

## 🔑 第三步：登录抖音（一次性）

1. 打开新标签页
2. 地址栏输入 `creator.douyin.com` 回车
3. 用**手机抖音扫码**登录
4. 登进去后**关掉标签页也没事**

登录状态会保存**几周到几个月**，正常用不需要重登。**只有以下情况才要重登**：
- 清过 Chrome 缓存
- 抖音强制下线
- 换电脑

---

## 🎯 第四步：开始下载

1. 点 Chrome 右上角**插件图标** 🎵
2. 弹出的小窗里选：
   - **时间窗**：最近 24 小时 / 7 天 / 30 天
   - **数量**：前 10 / 20 / 30 首
3. 点 **「开始下载」**
4. 看进度：
   - 插件图标右下角会跳数字：`1/20 → 2/20 → ...`
   - 弹窗里会显示当前正在下的歌名
5. 完成后图标显示绿色 ✓，弹通知「XX 首下载完成」

---

## 📁 找到你的 MP3

**Windows**：
```
C:\Users\你的用户名\Downloads\douyin-bgm\2026-08-06_1543\
```
（就是普通的「下载」文件夹里，会有个 `douyin-bgm` 子文件夹）

**Mac**：
```
/Users/你的用户名/Downloads/douyin-bgm/2026-08-06_1543/
```
（访达 → 下载 → `douyin-bgm`）

每次跑一次会新建一个以时间命名的子文件夹，不会覆盖以前的。

**快捷方式**：下载完成后，插件弹窗里会多出一个「**打开下载文件夹**」按钮，点它 Chrome 会直接跳到那个文件夹。

---

## 🩹 常见问题（遇到再看）

### Q1：找不到「开发者模式」开关？
- 你的 Chrome 版本太老了，升级到最新版：<https://www.google.com/chrome/>

### Q2：加载插件时报错「Manifest file is missing or unreadable」？
- 说明你选错文件夹了 —— 要选**包含 `manifest.json` 文件的那个文件夹**（就是解压出来的 `douyin-bgm-extension`），不要选它上一级或里面的子目录

### Q3：点开始没反应，弹「未登录或登录已过期」？
- 插件会自动打开抖音登录页 → 手机扫码 → 关掉页面 → 回来重点插件

### Q4：下载了但少几首？
- 少数音乐 CDN 偶发抽风，弹窗会显示「成功 18 / 失败 2」
- 稍后重新点一次「开始下载」，基本能补齐

### Q5：完全没反应，点图标弹窗都不出来？
- 去 `chrome://extensions/` 看看插件卡片是不是变灰了
- 灰了就点右下角的开关重新开启
- 还不行就点「移除」→ 重装一次

### Q6：Mac 装的话有什么区别？
- 步骤完全一样
- 唯一区别是「选择文件夹」按钮在 Mac 上可能叫「打开」/「Open」
- 下载文件夹路径是 `/Users/xxx/Downloads/` 而不是 `C:\Users\...`

---

## ⚠️ 版权提醒（重要）

抖音曲库的音乐**仅授权在抖音 APP 内使用**。

如果你打算把这些 MP3：
- ❌ 导出成 MP4 视频发到**其他平台**（B站、小红书、视频号等）
- ❌ 用于**商用**（品牌广告、带货视频）
- ❌ 交给**火山 Kickart 等混剪 API** 出片

**都需要自己核对每首歌的商用授权！** 否则可能被平台下架或版权方索赔。

**安全做法**：把下载的 MP3 只当**选曲参考**（听旋律 / 找灵感 / 记曲名），真正要用的时候去正版音乐库（光厂、Artlist、Epidemic Sound 等）找有商用授权的替代。

本工具**只做数据采集，版权由你自己把关**。

---

## 🔄 收到新版本怎么更新

1. 解压新的 zip 覆盖旧的文件夹（或删掉旧的重解压）
2. 打开 `chrome://extensions/`
3. 找到「抖音BGM采集器」卡片，点右下角的**刷新图标 ↻**
4. 完成

---

## ❓ 还有问题？

联系发你插件的人，把这两样发给他：
1. 你在哪一步卡住了
2. 页面上具体报什么错的截图

搞定 🎉
